const ModeSelect = () => {
  return (
    <div className="Modes">
      <div id="selectTeacher">Click to select Teacher Mode</div>
      <div id="selectStudent">Click to select Student Mode</div>
    </div>
  );
};

export default ModeSelect;
